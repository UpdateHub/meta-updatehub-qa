OpenEmbedded/Yocto Project UpdateHub QA metadata
================================================

This layer provides the metadata used for regression and as Quality
Assurance tests for UpdateHub.

By no means it is intended to be used by end users even though it can
be used as reference when doing new platform integrations.

Dependencies
------------

This layer depends on:

  URI: git://github.com/UpdateHub/meta-updatehub
  branch: pyro
